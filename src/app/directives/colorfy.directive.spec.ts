import { ColorfyDirective } from './colorfy.directive';

describe('ColorfyDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorfyDirective(null);
    expect(directive).toBeTruthy();
  });
});
